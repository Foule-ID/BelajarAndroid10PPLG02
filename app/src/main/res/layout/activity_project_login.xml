<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:backgroundTint="#000108"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- Judul -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lanjutkan setelah Masuk"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="120dp"/>

    <!-- Subtitle -->
    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Buat profil,buat videomu sendiri"
        android:textSize="14sp"
        android:textColor="#9E9E9E"
        android:gravity="center"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.8"/>

    <!-- Button Google -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGoogle"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:text="Lanjutkan dengan Google"
        android:textAllCaps="false"
        android:textColor="@android:color/black"
        app:iconGravity="textStart"
        app:iconPadding="12dp"
        app:strokeWidth="1dp"
        app:strokeColor="#DDDDDD"
        app:cornerRadius="8dp"
        android:backgroundTint="@android:color/white"
        android:layout_marginTop="40dp"
        app:layout_constraintTop_toBottomOf="@id/tvSubtitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.85"/>

    <ImageView
        android:id="@+id/Google"
        app:layout_constraintTop_toBottomOf="@+id/tvSubtitle"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="46dp"
        android:src="@drawable/Google1"
        android:layout_width="100px"
        android:layout_height="100px"/>

    <!-- Button Facebook -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnFacebook"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:text="Lanjutkan dengan Facebook"
        android:textAllCaps="false"
        android:textColor="@android:color/black"
        app:iconGravity="textStart"
        app:iconPadding="12dp"
        app:strokeWidth="1dp"
        app:strokeColor="#DDDDDD"
        app:cornerRadius="8dp"
        android:backgroundTint="@android:color/white"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnGoogle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.85"/>

    <ImageView
        app:layout_constraintTop_toBottomOf="@id/btnGoogle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="75px"
        android:layout_marginEnd="750px"
        android:src="@drawable/Facebook_Logo_2023"
        android:layout_width="70px"
        android:layout_height="70px"/>

    <!-- Login alternatif -->
    <TextView
        android:id="@+id/tvAltLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="atau gunakan Telepon | Line"
        android:textColor="#9E9E9E"
        android:textSize="13sp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/btnFacebook"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Privacy -->
    <TextView
        android:id="@+id/tvPrivacy"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Masuk berarti Anda menyetujui Ketentuan Layanan dan Kebijakan Privasi SnackVideo."
        android:textSize="12sp"
        android:textColor="#9E9E9E"
        android:gravity="center"
        android:layout_marginBottom="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.85"/>

</androidx.constraintlayout.widget.ConstraintLayout>
